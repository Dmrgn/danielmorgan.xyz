import { Button } from "../ui/button";

export default function RandomButton() {

    function onClick() {

    }

    return <Button className="cursor-pointer" onClick={onClick}>Click Me!</Button>
}
